[project]
name = "mcp-spellingbee"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.3.0",
]

[project.scripts]
spellingbee = "mcp_spellingbee.index:main"  # This uses underscores for the module path

[tool.setuptools.package-data]
"mcp_spellingbee" = ["corpora.txt"]  # Use underscores here, not hyphens