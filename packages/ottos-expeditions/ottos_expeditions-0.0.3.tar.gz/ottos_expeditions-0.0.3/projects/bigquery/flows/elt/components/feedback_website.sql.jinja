WITH feedback_website AS (
    SELECT
        *
    FROM
        {{ ref(
            "read_feedback_website",
            reshape={
                "time": {
                    "column": "timestamp",
                    "granularity": "month"
                }
            }
        ) }}
)
SELECT
    *
FROM
    feedback_website
ORDER BY
    timestamp DESC
