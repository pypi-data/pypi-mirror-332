# Template for `pyproject.toml`, where will be inserted the project name and dependencies
PYPROJECT_TEMPLATE = """[project]
name = "{project_name}"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = {dependencies}
"""
