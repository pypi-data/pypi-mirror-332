[project]
name = "hex-mcp"
version = "0.1.0"
description = "A MCP server for Hex"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
 "backoff>=2.2.1",
 "mcp>=1.3.0",
]
[[project.authors]]
name = "Franccesco Orozco"
email = "franccesco@codingdose.info"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
hex-mcp = "hex_mcp:main"
