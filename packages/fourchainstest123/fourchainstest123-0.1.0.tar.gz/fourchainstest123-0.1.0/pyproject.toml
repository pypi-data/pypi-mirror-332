[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fourchainstest123"
version = "0.1.0"
description = "A simple test package"
readme = "README.md"
requires-python = ">=3.10.15"
license = {text = "MIT"}
authors = [
    {name = "fourchains", email = "fourchains.work@gmail.com"}
]
dependencies = []

[project.urls]
"Homepage" = "https://github.com/fourchains/fourchainstest"
