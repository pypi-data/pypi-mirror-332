[project]
name = "async-task-scheduler"
version = "0.1.0"
description = "An async task scheduler framework"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"