[package]
name = "fuzzydate"
version = "0.5.5"
edition = "2021"

[lib]
name = "fuzzydate"
crate-type = ["cdylib"]

[dependencies]
chrono = "0.4.40"
pyo3 = { version = "0.23.5", features = ["chrono"] }
