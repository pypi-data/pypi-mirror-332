[project]
name = "northguard"
version = "0.0.1"
description = "A Python client for a pubsub system."
authors = [
    {name = "jonb"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
