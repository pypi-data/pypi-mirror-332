[project]
name = "leizha-demo-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "leizha", email = "none@none.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
leizha-demo-app = "leizha_demo_app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
