AGE AND GENDER DETECTION USING PYQT UI
 This is a simple demograaphy detection using PyQt UI. It uses the deepface model to detect the age and gender of the person. The model is trained on the dataset.
