# Task
Please draft rating dimensions for the topic: "{{ topic }}".
Return the dimensions{{#if criteria_count}} with length of {{ criteria_count }}{{/if}} in JSON format as a list of strings within code block.


# Example:
returning dimensions{{#if criteria_count}} with length of 3{{/if}} for the topic "Nice icecream"

----- Start of response example -----
```json
[
    "outlook",
    "taste",
    "texture"
]
```
----- End of response example -----


# Warning
- Please ensure that the dimensions are clear and concise.
- The response SHALL be returned as a JSON object within the codeblock.
- No additional Explanation is needed.
{{#if criteria_count }}- You must return exactly {{ criteria_count }} dimensions.{{/if}}