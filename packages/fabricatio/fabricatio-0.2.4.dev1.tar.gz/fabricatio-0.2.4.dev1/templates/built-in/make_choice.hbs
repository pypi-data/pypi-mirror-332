# Instruction:
{{instruction}}

# Options:
{{#each options}}
- {{briefing}}

{{/each}}

# Task:

Please choose the most appropriate option(s) from the list above. Return your choice as a JSON array of the selected option names.

Example:
----- Start of response example -----
```json
["option1", "option2"]
```
----- End of response example -----

# Warnings:

- Note that All items in list you returned **MUST** be contained in [{{#each options}}"{{name}}", {{/each}}], No extra items are allowed.
- Your response shall not have any explanation, only the JSON array of selected option names.
{{#if k}}- The array returned MUST have a length of **{{k}}**.{{/if}}
