[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "twitchpy"
version = "1.3.2"
description = "TwitchPy is a Python package for using the Twitch's API and create bots for interacting with their IRC chats."
readme = "README.md"
authors = [{ name = "DaCasBe", email = "dacasbe97@gmail.com" }]
license = { file = "LICENSE" }
keywords = []
dependencies = [
    "requests"
]
requires-python = ">=3.10"

[project.urls]
Homepage = "https://github.com/DaCasBe/TwitchPy"
