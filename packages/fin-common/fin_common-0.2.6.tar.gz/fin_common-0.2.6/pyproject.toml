[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fin-common"
version = "0.2.6"
authors = [
    { name = "Tommy Pang", email = "tommypang04@gmail.com" }
]
description = "A shared common module for FinForAll services."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = []  # Add dependencies if needed

[tool.setuptools.packages.find]
where = ["."]
