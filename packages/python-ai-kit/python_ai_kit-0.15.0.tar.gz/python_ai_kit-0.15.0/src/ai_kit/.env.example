# Example LOCAL .env file
# NOTE: you can add GLOBAL .env vars to ~/.ai-kit.env, which was auto created when you ran `ai-kit init`.
# To access the global env file, just run `ai-kit env` in your terminal. then, click on the path to open the file!
# Local vars will automatically override global vars.

# Required API Key
OPENAROUTER_API_KEY=

# Extra API Keys (optional)
# If you add one of these, ai-kit will use the provider directly instead of OpenRouter
GROQ_API_KEY=
TOGETHER_API_KEY=
GEMINI_API_KEY=

# adds reranking to the web search command
COHERE_API_KEY= 
