[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "creduse"
dynamic = ["version"]
description = "Python SDK for Creduse"
readme = "README.md"
requires-python = ">=3.13"
license = {text="MIT License"}
authors = [{name="Creduse", email="support@creduse.com"}]
dependencies = [
    
    "httpx>=0.28.1",
    "pydantic>=2.10.6",
]

[tool.hatch.version]
path = "creduse/__version__.py"

[tool.hatch.build.targets.sdist]
ignore-vcs = true
packages = ["creduse"]
exclude = ["tests", "examples", ".vscode", ".git", "__pycache__", ".gitignore", ".ruff_cache", ".python-version"]

[tool.hatch.build.targets.wheel]
ignore-vcs = true
packages = ["creduse"]
exclude = ["tests", "examples", ".vscode", ".git", "__pycache__", ".gitignore", ".ruff_cache", ".python-version"]

[dependency-groups]
dev = [
    "hatch>=1.14.0",
    "ruff>=0.9.4",
    "pytest>=8.3.4",
    "python-dotenv>=1.0.1",
    "pytest-mock>=3.14.0",
    "ipykernel>=6.29.5",
    "pytest-asyncio>=0.25.3",
]

