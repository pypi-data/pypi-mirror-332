name: Lint

on:
  push:
  pull_request:

jobs:
  lint:
    name: Lint with ruff
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v3.2.1
        with:
          src: ./src/lumon/
