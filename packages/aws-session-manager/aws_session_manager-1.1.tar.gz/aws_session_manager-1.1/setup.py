# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

setup(
    name='aws_session_manager',
    version='1.1',
    description='A command line UI to manage AWS Session Manager sessions',
    packages=find_packages(),
)
