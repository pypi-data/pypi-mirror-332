[build-system]
requires = ["flit_core"]
build-backend = "flit_core.buildapi"

[project]
name = "gguf-web"
authors = [{name = "calcuis", email = "info@calcu.io"}]
description = "gguf web (frontend)"
readme = "README.md"
license = {file = "LICENSE"}
classifiers = ["License :: OSI Approved :: MIT License"]
dynamic = ["version"]

[project.urls]
GitHub = "https://github.com/calcuis/gguf-web"
Homepage = "https://gguf.org"