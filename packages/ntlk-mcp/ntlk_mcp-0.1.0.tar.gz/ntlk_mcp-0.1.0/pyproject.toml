[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ntlk-mcp"
version = "0.1.0"
description = "This is broken, use at your own risk"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.3.0",
]

[tool.setuptools.package-data]
"server" = ["corpora.txt"]