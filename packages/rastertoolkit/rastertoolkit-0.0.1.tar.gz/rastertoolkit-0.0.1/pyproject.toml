[project]
name = "rastertoolkit"
version = "0.0.1"
description = "Python scripts for working with shapefiles and rasterized population data"
authors = [
  { name="Katherine Rosenfeld"},
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["rastertoolkit*"]
