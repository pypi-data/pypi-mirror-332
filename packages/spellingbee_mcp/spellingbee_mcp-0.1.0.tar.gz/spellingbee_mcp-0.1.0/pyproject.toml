[project]
name = "spellingbee_mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.3.0",
]

[project.scripts]
spellingbee_mcp = "spellingbee_mcp.index:main"

[tool.setuptools.package-data]
"spellingbee_mcp" = ["corpora.txt"]