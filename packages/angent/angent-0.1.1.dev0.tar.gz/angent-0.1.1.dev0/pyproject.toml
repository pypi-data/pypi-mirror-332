[project]
name = "angent"
version = "0.1.1.dev"
description = "Educational Multi-Agent Scaffold"
readme = "README.md"
authors = [{ name = "Gus", email = "contact@memobase.io" }]
requires-python = ">=3.11"
dependencies = ["drive-flow>=0.0.1", "openai>=1.65.4"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
