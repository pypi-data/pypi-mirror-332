[tool.poetry]
name = "my-mel"
version = "0.2.0"
description = "A Mel cat clone"
authors = ["Lais <your.email@example.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "mel"}]

[tool.poetry.dependencies]
python = "^3.8"
click = "^8.1.3"

[tool.poetry.group.dev.dependencies]
pytest = "^7.3.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
mel = "mel.cli:main"